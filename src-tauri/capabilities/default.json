{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability — core IPC only; no filesystem, shell, or opener plugins.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
